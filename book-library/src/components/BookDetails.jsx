import React from "react";

export default ({id}) => {
    return (
        <div>This is a placeholder for Book ID {id}. Safely overwrite whenever the actual front-end is being created.</div>
    );
}